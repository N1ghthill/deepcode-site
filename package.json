{
  "name": "deepcode-site",
  "version": "1.0.0",
  "private": true,
  "description": "Landing page for DeepCode — a terminal-first AI coding agent",
  "scripts": {
    "dev": "npx serve .",
    "start": "npx serve ."
  }
}
